# Backend API base URL — must match the running backend port
VITE_API_BASE_URL=http://localhost:5004/api

# ── Playwright E2E (only needed for running e2e tests) ──────────────
# Pre-seeded test accounts. Add these users via "npm run seed" or manual SQL.
E2E_USER_EMAIL=demo@healthguide.app
E2E_USER_PASSWORD=Demo1234!
E2E_ADMIN_EMAIL=admin@healthguide.app
E2E_ADMIN_PASSWORD=Admin1234!
PLAYWRIGHT_BASE_URL=http://localhost:5173
